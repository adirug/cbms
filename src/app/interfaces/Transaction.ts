// import { Customer } from "./Customer";

export interface Transaction {
    txnId?: number;
    txnDate?: number;
    txnType?: string;
    // customer?:Customer;
    // transferedToAccount?:number;
}